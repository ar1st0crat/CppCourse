// Пример перечисления
// (предпочтительный вариант, начиная с с++11)
enum class Color
{
    BLACK,
    RED,
    GREEN,
    BLUE,
    GRAY,
    WHITE,
    COUNT
};

/* в старом с++ пишется так:
 *

enum Color
{
    BLACK,
    RED,
    //...
};

Color color = BLACK;

*/
